version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: wishlist-postgres
    environment:
      - POSTGRES_DB=wishlist
      - POSTGRES_USER=wishlist
      - POSTGRES_PASSWORD=wishlist
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # - ./backend/migrations:/docker-entrypoint-initdb.d/  # Disabled to avoid migration conflicts
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wishlist -d wishlist"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: